edition = "2023";

package api.v1;

// PublicErrorInfo is the structured error body returned to API clients.
message PublicErrorInfo {
  // Stable machine-readable error code.
  string code = 1;
  // Human-readable error description.
  string message = 2;
}
